@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Aura Inventario</h1>
    @*<p class="lead">TODO.</p>*@
    @{ 
        if (!(Session["UserID"]==null)) {
            <p class="lead">Bienvenido: @Session["UserName"].ToString()</p>
        } else {
            <p><a href="" data-toggle="modal" data-target="#LoginModal" class="btn btn-primary btn-lg">Login &raquo;</a></p>
        }
    }
   
</div>

<div class="row">

</div>