@using (Ajax.BeginForm("UploadExcel", "INV_PC", new AjaxOptions() { HttpMethod = "POST", UpdateTargetId= "result", OnSuccess= "getresult()" }, new { enctype = "multipart/form-data" })) {
    <div id="myModal" class="modal fade animated bounceInDown ">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title">Ingreso Masivo de Inventarios Pc</h4>
                </div>
                <div class="modal-body">
                    <p>Descargar Formato:</p>
                    <a class="btn btn-primary" href="/INV_PC/DownloadExcel/">Descargar</a>
                    <hr />
                    <p>Cargar Excel:</p>
                    <input type="file" id="FileUpload" name="FileUpload" class="btn btn-primary" style="max-width:500px;border-radius:30px;"  />
                    <span class="help-block">Esta accion tiene un limite de 200 valores.</span>
                    <hr />
                    <div id="result" class="text text-danger" style="white-space: pre-line"></div>                    
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal" id="cer">Cerrar</button>
                    <button type="submit" class="btn btn-primary" id="submitupload" onsubmit="">Subir Archivo</button>
                </div>
            </div>
        </div>
    </div>
}



